<UserControl x:Name="userControl" x:Class="DeepglintChart.UserPie"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="using:DeepglintChart"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="grid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style x:Key="canvasStyle" TargetType="Canvas" >
            </Style>
        </Grid.Resources>
        <Grid x:Name="gridSeries" HorizontalAlignment="Stretch" Grid.Row="1" VerticalAlignment="Stretch"/>
        <!--<Viewbox x:Name="viewbox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">-->
        <Canvas x:Name="canvas" Width="370" Height="300" >
            
        </Canvas>
        
        <!--</Viewbox>-->

    </Grid>
</UserControl>
